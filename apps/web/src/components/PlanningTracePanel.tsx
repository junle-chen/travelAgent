import type { TripState } from '../lib/models';

interface PlanningTracePanelProps {
  trip: TripState;
}

export function PlanningTracePanel({ trip: _trip }: PlanningTracePanelProps) {
  return null;
}
